server:
  port: 8080

users:
  service:
    url: http://localhost:8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3307/plazoleta
    username: anke
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
appDescription: "Pragma Foodcourt"
appVersion: "1.0.0"
